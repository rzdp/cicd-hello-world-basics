pipeline {

    agent any

    stages {

        stage("build") {
            echo 'Building the dev application..'
        }

        stage("test") {
            echo 'Testing the dev application..'
        }

        stage("deploy") {
            echo 'Deploying the dev application..'
        }
    }
}