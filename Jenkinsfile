pipeline {

    agent any

    stages {

        stage("build") {
            echo 'Building the test application..'
        }

        stage("test") {
            echo 'Testing the test application..'
        }

        stage("deploy") {
            echo 'Deploying the test application..'
        }
    }
}
