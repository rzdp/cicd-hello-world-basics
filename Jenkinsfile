pipeline {

    agent any

    stages {

        stage("build") {
            echo 'Building the master application..'
        }

        stage("test") {
            echo 'Testing the master application..'
        }

        stage("deploy") {
            echo 'Deploying the master application..'
        }
    }
}
